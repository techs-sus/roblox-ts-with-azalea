# roblox-ts-test

roblox-ts template for script builders

## Workflow

If your shell supports & parallelization:

```bash
bun run watchAzalea & bun run watch
```

If not, run `bun run watchAzalea` and `bun run watch` in seperate terminals.
